# Семейное ядро / FamilyCore

Лендинг продукта «Семейное ядро» — системы семейных договорённостей с AI-брифингами.

🌐 **Live:** [familycore.app](https://familycore.app)

---

## Структура проекта

```
familycore/
├── index.html              # Русская версия (основная)
├── en.html                 # English version
├── style.css               # Общие стили (обе версии)
├── favicon.svg             # Иконка сайта
├── google-apps-script.js   # Скрипт для приёма заявок (Google Apps Script)
├── CNAME                   # Домен для GitHub Pages
└── README.md
```

## Стек

- **HTML + CSS + vanilla JS** — статический сайт, без сборки
- **Google Fonts** — DM Serif Display + Onest
- **Google Apps Script** — приём заявок с формы → Google Таблица + email-уведомление
- **Хостинг** — GitHub Pages (или любой статический хостинг)

## Форма обратной связи

Заявки с формы «Ранний доступ» отправляются в Google Apps Script, который:
1. Записывает строку в Google Таблицу (дата, имя, email, сообщение)
2. Отправляет email-уведомление на `contact@familycore.app`

### Настройка

1. Создайте Google Таблицу
2. **Расширения → Apps Script** → вставьте содержимое `google-apps-script.js`
3. При необходимости измените `NOTIFY_EMAIL` на свою почту
4. **Развернуть → Новое развёртывание → Веб-приложение**
   - Выполнять от: Меня
   - Доступ: Все
5. Скопируйте URL развёртывания
6. В `index.html` и `en.html` замените значение `FORM_URL` на полученный URL
7. Для проверки откройте URL в браузере — должно отобразиться «OK»

> **Важно:** после каждого изменения кода в Apps Script нужно делать **новое развёртывание**, а не редактировать существующее.

## Локальная разработка

Любой статический сервер:

```bash
# Python
python3 -m http.server 8000

# Node.js
npx serve .
```

Открыть `http://localhost:8000`

## Деплой на GitHub Pages

1. Создайте репозиторий на GitHub
2. Залейте файлы
3. **Settings → Pages → Source:** Deploy from a branch → `main` / `root`
4. Добавьте файл `CNAME` с содержимым `familycore.app`
5. Настройте DNS: CNAME-запись `familycore.app` → `<username>.github.io`

## Языки

| Версия | Файл | Навбар | Бренд |
|--------|-------|--------|-------|
| Русская | `index.html` | Семейное ядро | Семейное ядро |
| English | `en.html` | FamilyCore | FamilyCore |

Переключатель языка — кнопка `EN` / `RU` в навигации.

## Лицензия

Проприетарный проект. Все права защищены.